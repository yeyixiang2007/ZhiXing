# 校园智能路径规划导航系统 - 后续执行清单

> 基于 `docs/PRD.md` 与任务安排表整理，按当前项目进度继续推进。

## 已完成（当前基线）
- [x] 项目结构与开发环境初始化（`pom.xml`、`scripts`、`.vscode`）
- [x] 核心实体第一版：`Vertex`、`Edge`、`CampusGraph`
- [x] 基础编译运行打通（`build.ps1` / `run.ps1`）

## 后续清单（按执行顺序）

### 1. 完成核心建模收口（M1）
- [x] 补齐领域对象：`PathResult`、`User/Admin/NormalUser`、`Role`
- [x] 补齐模型校验规则（ID 唯一、权重 > 0、空值校验）
- [x] 增加模型层单元测试覆盖核心边界条件

### 2. 实现路径规划主链路（M2）
- [x] 定义算法接口：`PathPlanningStrategy`
- [x] 实现 `DijkstraStrategy`（P0）
- [x] 支持禁行过滤与不可达判定（P0）
- [x] 输出路径详情：总距离、途经点、分段距离（P0）

### 3. 实现导航指引模块
- [x] 生成文字分步导航（P0）
- [x] 预计耗时计算（按步行速度）
- [x] 路径详情展示格式统一（控制台输出模板）

### 4. 实现地图管理服务
- [x] `MapService`：地点/道路增删改查（P0）
- [x] 禁行路段设置与解除（P1）
- [x] 地点类型筛选（P1）

### 5. 实现持久化与启动加载
- [x] 设计并落地数据文件：`vertex.json`、`edge.json`、`user.json`
- [x] `PersistenceService`：加载/保存/备份恢复（P0）
- [x] 文件异常兜底：损坏或缺失时加载默认地图（P0）

### 6. 实现权限与登录
- [x] 角色权限控制（普通用户/管理员）（P0）
- [x] 管理员账号登录（P1）
- [x] 密码本地加密存储（P1）

### 7. 实现控制台交互主流程（M3）
- [x] 用户菜单：路径查询、地点查看（P0）
- [x] 管理员菜单：地图管理、禁行设置（P0/P1）
- [x] 非法输入友好提示与重试（P0）

### 8. 联调与测试（M3 -> M4）
- [x] 功能测试：P0 全量回归
- [x] 异常测试：非法输入、不可达、文件异常、权限越界
- [x] 性能测试：路径计算 <= 1s、启动 <= 2s
- [x] 修复缺陷并复测闭环

### 9. 文档与答辩材料收口（M4）
- [ ] 更新课程设计报告与 UML 为最终实现版
- [ ] 补充测试报告、使用说明、演示脚本
- [ ] 准备答辩 PPT（需求 -> 设计 -> 实现 -> 测试 -> 结果）

### 10. GUI页面设计与实现（M5，覆盖控制台全功能）

#### 10.1 GUI总体页面设计
- [x] 主窗口框架：顶部标题栏 + 左侧功能导航 + 中间工作区 + 底部状态栏
- [x] 页面路由结构：`用户模式`、`管理员模式`、`系统设置` 三大入口
- [x] 统一样式规范：按钮/表格/表单/提示框样式与字体规范
- [x] 通用交互组件：确认弹窗、错误提示弹窗、加载中遮罩、操作结果提示条

#### 10.2 功能等价映射（对齐控制台全能力）
- [x] 用户菜单能力映射：
- [x] 路径查询页（起点终点选择、查询按钮、路径详情、分步导航）
- [x] 地点查看页（全部地点列表、按类型筛选）
- [x] 管理员菜单能力映射：
- [x] 管理员登录弹窗（账号密码校验、失败重试）
- [x] 地点管理页（新增/修改/删除/列表）
- [x] 道路管理页（新增/修改/删除/列表）
- [x] 禁行管理页（设置禁行/解除禁行/禁行状态展示）
- [x] 地图概览页（地点数、道路数、禁行道路数统计）

#### 10.3 GUI任务模块拆分（可并行）
- [x] 模块A：应用骨架与导航容器
- [x] 任务A1：创建 `AppLauncher` 与 `MainView`（主窗口）
- [x] 任务A2：实现左侧导航与页面切换机制（Scene/Pane 切换）
- [x] 任务A3：实现全局状态栏与统一消息提示接口
- [x] 模块B：用户功能页面
- [x] 任务B1：`PathQueryView`（路径查询 + 结果展示）
- [x] 任务B2：`PlaceBrowseView`（地点列表 + 类型筛选）
- [x] 任务B3：路径详情组件（总距离、耗时、分段距离、分步导航）
- [x] 模块C：管理员功能页面
- [x] 任务C1：`AdminLoginDialog`（登录、失败重试、权限提示）
- [x] 任务C2：`VertexManageView`（地点增删改查）
- [x] 任务C3：`RoadManageView`（道路增删改查）
- [x] 任务C4：`ForbiddenManageView`（禁行设置/解除）
- [x] 任务C5：`OverviewDashboardView`（统计卡片）
- [x] 模块D：控制器与服务适配层
- [x] 任务D1：`NavigationController` 对接 `NavigationService`
- [x] 任务D2：`MapController` 对接 `MapService`
- [x] 任务D3：`AuthController` 对接 `AuthService`
- [x] 任务D4：统一异常转用户友好提示（输入错误/不可达/权限不足/文件异常）
- [x] 模块E：数据与状态同步
- [x] 任务E1：页面操作后自动触发 `PersistenceService.saveGraph/saveUsers`
- [x] 任务E2：启动时加载 `loadGraphOrDefault/loadUsersOrDefault` 并刷新页面
- [x] 任务E3：备份恢复入口（管理员工具菜单）
- [x] 模块F：GUI联调测试
- [x] 任务F1：用户主流程联调（查询/筛选）
- [x] 任务F2：管理员主流程联调（登录/地点/道路/禁行）
- [x] 任务F3：异常流程测试（非法输入、不可达、权限越界、文件异常）
- [x] 任务F4：性能验证（GUI启动 <= 2s、查询 <= 1s）

#### 10.4 验收标准（GUI）
- [x] 与控制台功能一一对应，无缺项
- [x] 关键操作（查询、增删改）在 GUI 中 3 步内完成
- [x] 所有异常均有可读提示，不出现未捕获异常弹栈
- [x] 通过完整回归脚本与手工冒烟测试

## 里程碑对应
- [ ] M1：核心模型可用（建模完成 + 测试通过）
- [ ] M2：最短路径流程跑通（含不可达与禁行）
- [ ] M3：系统端到端可演示
- [ ] M4：文档齐套并满足验收标准
- [ ] M5：GUI版本达到与控制台等价能力并完成验收
