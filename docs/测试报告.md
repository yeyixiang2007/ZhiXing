# 测试报告（M3 联调与测试）

## 1. 测试范围
- 功能回归（P0）：路径查询、地点查看、管理员地图管理、持久化读写、登录认证
- 异常测试：非法输入重试、不可达路径、文件损坏兜底、权限越界
- 性能测试：路径计算耗时、启动加载耗时

## 2. 测试方式
- 执行脚本：`scripts/qa.ps1`
- 测试入口：`com.zhixing.navigation.app.QualityCheckRunner`

## 3. 测试结果
- 总用例数：7
- 通过：7
- 失败：0

### 3.1 功能回归
- P0 功能回归：通过

### 3.2 异常测试
- 非法输入友好提示与重试：通过
- 不可达路径提示：通过
- 文件异常兜底（损坏/缺失）：通过
- 权限越界拦截：通过

### 3.3 性能测试
- 路径计算（200 顶点/500 边）：`0 ms`（<= 1s，达标）
- 启动加载：`1 ms`（<= 2s，达标）

## 4. 缺陷修复与闭环
本轮联调发现并修复 1 个稳定性缺陷，并完成复测：

1. 输入流意外结束时控制台程序会抛出异常退出  
- 修复文件：`src/main/java/com/zhixing/navigation/app/ConsoleApplication.java`  
- 修复方案：新增输入流结束检测与友好退出提示（不再抛栈崩溃）  
- 复测结论：通过

此外补充了测试隔离能力：
- `src/main/java/com/zhixing/navigation/app/Main.java` 支持通过环境变量 `ZHIXING_DATA_DIR` 指定数据目录，避免测试污染正式数据。

